function charCount(str) {
	// make object to return at end
	// loop over string and count each alphanumeric char
	// add property of char: count pair if it doesn't exist
	// increment count if pair exists
	// return object
}
